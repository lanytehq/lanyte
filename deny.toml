[advisories]
db-path = "~/.cargo/advisory-db"
unmaintained = "workspace"
yanked = "warn"
ignore = [
    # sysprims-core pulls `time`; 0.3.47+ needs rustc 1.88. The advisory is RFC
    # 2822 stack exhaustion on untrusted input, which this workspace does not parse.
    "RUSTSEC-2026-0009",
]

[licenses]
allow = [
    "MIT",
    "MIT-0",
    "Apache-2.0",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "Unicode-3.0",
    "Zlib",
    "CDLA-Permissive-2.0",
]
confidence-threshold = 0.8

[bans]
multiple-versions = "warn"
wildcards = "deny"

[sources]
unknown-registry = "deny"
unknown-git = "allow"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
allow-git = []
